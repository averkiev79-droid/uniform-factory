# 🖼️ Проблема: Пропало фото на главной странице

## Симптомы
- Главная страница загружается, но нет hero-изображения
- Вместо фото пустое место или placeholder

## Причина
В базе данных `app_settings` таблица хранит путь к hero_image. Иногда туда попадает старый URL со старого домена (например, от предыдущего деплоя).

## ✅ Решение (быстрое)

### Способ 1: Автоматический скрипт
```bash
cd /app/backend
python3 fix_hero_image.py
sudo supervisorctl restart backend
```

### Способ 2: Вручную через базу данных
```bash
cd /app/backend
sqlite3 avik_uniform.db
```

В SQLite консоли:
```sql
-- Проверить текущее значение
SELECT hero_image FROM app_settings WHERE id='default';

-- Исправить на правильный путь
UPDATE app_settings 
SET hero_image = '/images/hero-main.jpg' 
WHERE id='default';

-- Выйти
.quit
```

Затем перезапустить backend:
```bash
sudo supervisorctl restart backend
```

### Способ 3: Через админ-панель
1. Откройте админ-панель: `/admin`
2. Войдите (пароль: `avik2024admin`)
3. Перейдите в раздел "Управление изображениями"
4. В поле "Hero изображение (главная страница)" введите: `/images/hero-main.jpg`
5. Нажмите "Сохранить настройки"

---

## 🔍 Диагностика

### Проверить текущее значение в базе:
```bash
cd /app/backend
python3 << 'EOF'
import sqlite3
conn = sqlite3.connect('avik_uniform.db')
cursor = conn.cursor()
cursor.execute("SELECT hero_image FROM app_settings WHERE id='default'")
print(f"hero_image: {cursor.fetchone()[0]}")
conn.close()
EOF
```

### Проверить через API:
```bash
curl -s "$(cat /app/frontend/.env | grep REACT_APP_BACKEND_URL | cut -d '=' -f2)/api/settings" | python3 -m json.tool | grep hero_image
```

**Правильное значение должно быть:**
```
"hero_image": "/images/hero-main.jpg"
```

**Неправильные значения (нужно исправить):**
```
"hero_image": "https://clothing-shop-36.preview.emergentagent.com/..."  ❌
"hero_image": "https://старый-домен.com/..."  ❌
"hero_image": ""  ❌
"hero_image": null  ❌
```

---

## 🛡️ Профилактика

### Чтобы проблема не повторялась:

1. **При загрузке изображений через админку** всегда проверяйте, что путь начинается с `/images/` или `/api/uploads/`

2. **После каждого обновления** проверяйте главную страницу

3. **Перед деплоем на production** запустите скрипт:
   ```bash
   cd /app/backend
   python3 fix_hero_image.py
   ```

4. **После переноса на Sweb.ru** сразу проверьте настройки:
   ```bash
   curl http://localhost/api/settings | python3 -m json.tool
   ```

---

## 📝 Технические детали

### Где хранятся настройки:
- **База данных:** `/app/backend/avik_uniform.db`
- **Таблица:** `app_settings`
- **Поле:** `hero_image`

### Как работает Hero компонент:
1. При загрузке страницы вызывается `apiService.getSettings()`
2. Получает `hero_image` из API
3. Если путь начинается с `/api/` — добавляет `BACKEND_URL`
4. Если путь начинается с `/images/` — использует из `public` папки
5. Если пусто или ошибка — использует fallback `/images/hero-main.jpg`

### Файл изображения:
```
/app/frontend/public/images/hero-main.jpg
```
Размер: ~76 KB
Разрешение: 800x800

---

## 🚨 Если скрипт не помог

### 1. Проверить, что файл существует:
```bash
ls -lh /app/frontend/public/images/hero-main.jpg
```

Если файла нет — восстановите из бэкапа или загрузите новый.

### 2. Очистить кэш браузера:
- Ctrl + F5 (Windows/Linux)
- Cmd + Shift + R (Mac)

### 3. Проверить логи backend:
```bash
tail -50 /var/log/supervisor/backend.err.log
```

### 4. Проверить логи frontend:
Откройте в браузере DevTools (F12) → Console
Ищите ошибки загрузки изображений.

---

## ✅ Чеклист решения проблемы

- [ ] Запустил `fix_hero_image.py`
- [ ] Перезапустил backend
- [ ] Проверил API endpoint `/api/settings`
- [ ] Очистил кэш браузера
- [ ] Открыл главную страницу
- [ ] Изображение отображается ✅

---

## 📞 Дополнительная помощь

Если проблема сохраняется после всех шагов:

1. Создайте issue с описанием:
   - Что показывает `/api/settings`
   - Что в базе данных
   - Скриншот главной страницы
   - Логи из консоли браузера

2. Временное решение:
   - Загрузите любое изображение через админ-панель
   - Или используйте изображение по URL

---

## 🎓 Полезные ссылки

- Скрипт исправления: `/app/backend/fix_hero_image.py`
- Hero компонент: `/app/frontend/src/components/Hero.jsx`
- API настроек: `/app/backend/services_sqlite.py` (строка 867)
- Админ-панель: `/admin` → Управление изображениями

---

**Дата создания:** 24 октября 2025  
**Последнее обновление:** 24 октября 2025
