# Инструкция по синхронизации между Emergent и sweb.ru

## Гибридный подход: Emergent + sweb.ru

### Схема работы

```
Emergent (разработка и тестирование)
    ↓
    ↓ (Deploy кнопка)
    ↓
Emergent Deploy (для быстрого доступа и тестирования)
    ↓
    ↓ (GitHub экспорт)
    ↓
sweb.ru (основной продакшн)
```

---

## Первоначальная настройка (один раз)

### Шаг 1: Deploy на Emergent
1. В чате нажмите кнопку **"Deploy"**
2. Дождитесь завершения (2-5 минут)
3. Получите URL типа: `https://uniform-factory-xxx.emergent.app`
4. Это ваша тестовая версия для проверки обновлений

### Шаг 2: Настройка GitHub (для синхронизации)
1. Нажмите **"Save to GitHub"** в чате
2. Если не подключен GitHub:
   - Профиль → Connect GitHub
   - Авторизуйтесь
3. Создайте репозиторий `uniform-factory`
4. Код будет сохранен автоматически

### Шаг 3: Развертывание на sweb.ru
Следуйте инструкции из файла `DEPLOYMENT_SWEB_RU.md`:
1. Закажите VPS на sweb.ru
2. Подключитесь по SSH
3. Клонируйте репозиторий с GitHub
4. Выполните настройку (занимает 1-2 часа)

---

## Рабочий процесс: Как обновлять сайт

### Вариант А: Простое обновление без изменений кода

**На сервере sweb.ru:**
```bash
cd /var/www/uniform-factory
git pull origin main
./deploy-to-sweb.sh
```

Скрипт автоматически:
- Обновит код
- Перезапустит backend
- Пересоберет frontend
- Создаст бэкап

---

### Вариант Б: Обновление с новыми функциями

#### 1. Разработка на Emergent
Вы говорите мне: *"Нужно добавить галерею отзывов"*

Я делаю:
```
✅ Реализую функцию
✅ Тестирую здесь
✅ Сообщаю вам когда готово
```

#### 2. Тестирование на Emergent Deploy
```
✅ Вы проверяете на https://uniform-factory-xxx.emergent.app
✅ Убеждаетесь что всё работает
```

#### 3. Экспорт в GitHub
В чате нажимаете:
```
"Save to GitHub" → Обновить существующий репозиторий
```

#### 4. Обновление на sweb.ru
**На сервере sweb.ru через SSH:**
```bash
cd /var/www/uniform-factory
git pull origin main
./deploy-to-sweb.sh
```

**Или если нет Git:**
- Скачайте код через VS Code кнопку
- Загрузите на sweb.ru через SFTP (FileZilla)
- Запустите `./deploy-to-sweb.sh`

---

## Частые сценарии

### Сценарий 1: Мелкие правки (текст, стили)
```
Emergent (я правлю) 
    → Тестируете здесь
    → GitHub
    → sweb.ru (git pull + deploy)
Время: 10-15 минут
```

### Сценарий 2: Новая функция
```
Emergent (разработка + тестирование) 
    → Emergent Deploy (проверка)
    → GitHub  
    → sweb.ru (развертывание)
Время: 1-3 часа на разработку + 10 минут на деплой
```

### Сценарий 3: Срочный hotfix
```
Emergent (исправление)
    → Быстрая проверка
    → GitHub
    → sweb.ru (git pull + deploy)
Время: 5-10 минут
```

---

## Контроль версий: Две ветки

Рекомендую использовать две ветки в GitHub:

### `main` - продакшн (sweb.ru)
Стабильная версия для sweb.ru

### `dev` - разработка (Emergent)
Для тестирования новых функций

**Процесс:**
```bash
# На Emergent: работаем в dev ветке
git checkout dev
[делаем изменения]
git commit -m "Добавлена новая функция"
git push origin dev

# Тестируем на Emergent Deploy

# Когда всё ОК, мержим в main
git checkout main
git merge dev
git push origin main

# На sweb.ru подтягиваем main
git pull origin main
./deploy-to-sweb.sh
```

---

## Сравнение версий

|  | Emergent Deploy | sweb.ru |
|---|---|---|
| **Назначение** | Тестирование, демо | Основной продакшн |
| **URL** | .emergent.app | ваш-домен.ru |
| **Обновление** | 1 клик (Deploy) | git pull + скрипт |
| **Стоимость** | 50 кредитов/мес | 300-600₽/мес VPS |
| **Скорость** | Мгновенно | 10 минут |
| **Контроль** | Управляется Emergent | Полный контроль |
| **Бэкапы** | Автоматические | Настраиваете сами |
| **SSL** | Автоматически | Certbot (один раз) |

---

## Checklist синхронизации

Перед каждым обновлением на sweb.ru:

- [ ] Функция протестирована на Emergent
- [ ] Проверена на Emergent Deploy
- [ ] Код сохранен в GitHub
- [ ] Создан бэкап на sweb.ru (скрипт делает автоматически)
- [ ] Есть доступ к серверу sweb.ru
- [ ] Проверен статус сервисов перед обновлением

После обновления:

- [ ] Сайт открывается (http://ваш-домен.ru)
- [ ] Backend работает (проверить API)
- [ ] Frontend отображается корректно
- [ ] Изображения загружаются
- [ ] Формы отправляются
- [ ] Админ-панель доступна

---

## Команды для sweb.ru (шпаргалка)

```bash
# Переход в папку проекта
cd /var/www/uniform-factory

# Получить последние изменения
git pull origin main

# Автоматическое обновление
./deploy-to-sweb.sh

# Ручное обновление backend
cd backend
source venv/bin/activate
pip install -r requirements.txt
sudo supervisorctl restart uniform-backend

# Ручное обновление frontend
cd frontend
npm install
npm run build
sudo systemctl reload nginx

# Просмотр логов
tail -f /var/log/uniform-backend.out.log
tail -f /var/log/nginx/error.log

# Проверка статуса
sudo supervisorctl status
sudo systemctl status nginx

# Создать бэкап вручную
DATE=$(date +%Y%m%d_%H%M%S)
cp backend/avik_uniform.db /root/backups/db_$DATE.db
```

---

## Решение проблем

### Проблема: Git pull не работает
```bash
# Проверить статус Git
git status

# Сбросить локальные изменения
git reset --hard origin/main
git pull origin main
```

### Проблема: Backend не перезапускается
```bash
# Проверить логи
tail -n 50 /var/log/uniform-backend.err.log

# Проверить процесс
ps aux | grep uvicorn

# Убить процесс и перезапустить
sudo supervisorctl stop uniform-backend
sudo supervisorctl start uniform-backend
```

### Проблема: Frontend не обновляется
```bash
# Очистить кеш npm
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run build

# Очистить кеш браузера
# Ctrl+F5 в браузере
```

---

## Контакты

**Я (AI Agent):** Помогаю с разработкой на Emergent
**Поддержка Emergent:** support@emergent.ai
**Поддержка sweb.ru:** support@sweb.ru, 8 (800) 333-62-04

---

## Резюме

✅ **Разработка:** Emergent (быстро, удобно, я помогаю)
✅ **Тестирование:** Emergent Deploy (1 клик, мгновенно)
✅ **Продакшн:** sweb.ru (ваш домен, полный контроль)
✅ **Синхронизация:** GitHub + скрипт (10 минут)

Лучшее из двух миров! 🚀
